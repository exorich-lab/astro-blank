---
import Layout from '../layouts/main.astro';

export interface Props {
	title: string;
}

const { title = "Clean Page" } = Astro.props;
---
<html lang="en">
<head>
  <title>Fesome Site</title>
  <!-- Фавиконка с буквой "M" сгенерируется автоматически -->
</head>

<Layout content={{ title }}>
	<div class="min-h-screen">
		<!-- Ваш контент будет здесь -->
	</div>
</Layout>